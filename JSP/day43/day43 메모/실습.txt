1.
인코딩 설정
	모두 제거
	인코딩 필터로 변경

2.
커스텀 태그를 활용 하기. (최소 2개 이상)

하나의 기능을 구현하는데,
사용되는 코드의 길이가 너무 길면 다 이렇게 처리.

하나의 화면 이지만,
a 상태일 때랑, b 상태 일때의 화면 등으로,
상태에 따라 보여지는 화면 구성이 서로 다를때도,
이렇게 처리.

만약에 로그인 했니 ?
그러면 이렇게 ~

로그인 안했니 ?
그러면 이렇게 ~

이런 경우.

day43(customTag) 에서 





