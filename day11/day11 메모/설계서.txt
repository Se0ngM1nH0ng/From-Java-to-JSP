해야할 일

1. Pokemon 추상클래스 에서

game() 메서드의 기능을 구현 하기.

2. Pika 하위클래스 에서
추상메서드인 hello() 메서드를 오버라이딩 하기.

3. Turtle 하위클래스 에서
추상메서드인 hello() 메서드를 오버라이딩 하기.

4. main() 에서

1) Pika랑 Turtle 객체 생성 하기

2) 이름을 입력 받을 필요가 있나 ?
없는듯. 

그러면 
p.name 또는 t.name 을
하면 멤버변수에 직접 접근을 하는건데,
멤버변수를 다 private 처리를 해줬으므로,
접근이 불가능해서 호출을 할 수가 없다.

그래서, 이름값은 고정이니,
sysout(p.getName());
sysout(t.getName());
으로 이름을 출력.